package com.techelevator.pageObjects;

import org.openqa.selenium.WebDriver;

public class ParkResultsPageObject {
	
	private WebDriver webDriver; 

	public ParkResultsPageObject(WebDriver webDriver) {
		// TODO Auto-generated constructor stub
	}
	//Will use the object to check for the title of the page, done from the test file. No methods known to be needed now.
}
